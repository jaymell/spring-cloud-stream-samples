spring:
  cloud:
    stream:
      bindings:
        supplier-out-0:
          contentType: application/json+avro
          destination: my-random-topic2
      schema-registry-client:
        endpoint: http://localhost:8990
      schema:
        avro:
          schema-locations: classpath:avro/sensor.avsc
server.port: 9010